# 한화그룹 신입 채용 직무안내 챗봇 — 구현 스펙

> Claude Code 투입용 문서. 리포지토리 루트에 `SPEC.md` 또는 `CLAUDE.md`로 배치.

---

## 0. 프로젝트 개요

**목적**: 한화그룹 신입 지원자에게 계열사·부문·직무별 실무 내용과 요구 역량을 안내하는 대화형 챗봇.

**핵심 원칙**
1. 검색으로 나오는 정보의 재탕이 아니라, 교차 정보(부문 × 직군)와 개인화 진단으로 차별화한다.
2. 모든 답변은 인덱싱된 원본 문서에 근거한다. 근거 없는 생성은 금지한다.
3. 합격 가능성 예측, 커트라인, 학교·성별·연령 관련 판단은 시스템 레벨에서 차단한다.

**기술 스택 가정** (변경 시 이 절만 수정)
- Backend: Node.js / Express (ESM)
- LLM: Google Gemini API (`gemini-3.6-flash`, `backend/lib/llm.js`) — 2026-09에 비용 없는 로컬 Ollama로 잠시 전환했다가, 웹사이트로 배포하기 위해 클라우드 API로 되돌렸다(로컬 모델은 사용자 PC가 꺼지면 같이 죽어서 공개 배포가 안 됨).
- 웹 검색 그라운딩: **없음.** `generateGroundedText`는 `generateText`의 얇은 래퍼로 `sources`는 항상 빈 배열이다. §4.1(직무 설명)·§4.4(직무 이슈 분석)는 모델의 사전학습 지식만으로 답하며, 시스템 프롬프트로 "특정 기사·날짜·URL을 지어내지 말 것"을 지시하고 화면에는 실시간 정보가 아니라는 안내 문구를 노출한다. (필요해지면 Gemini `googleSearch` 툴로 실시간 그라운딩을 다시 켤 수 있다 — 별도 작업.)
- Vector DB: 없음 — §4.5 구현 범위에서는 ID 기반 JSON 직접 조회만 사용 (§2~§3 검색 파이프라인은 별도 단계로 보류)
- Frontend: React + Vite + TypeScript
- 데이터: JSON 파일 기반 (`/data`), 초기엔 DB 없이 시작
- 세션: 서버 메모리 Map (`backend/session/store.js`) — 재시작 시 초기화됨 (프로토타입 단계)
- 테스트: Vitest (`tests/*.test.js`) — pytest 대체

---

## 1. 데이터 모델 (최우선 구현 대상)

조직축과 기능축은 **계층이 아니라 매트릭스**다. 절대 하나의 트리로 합치지 말 것.

```
계열사(company) → 부문/사업부(division)      ... 조직축
직군(job_family) → 세부직무(job_role)        ... 기능축
                    ↓
              position (교차 셀) = 실제 채용 단위
```

### 1.1 조직 마스터 — `/data/organizations.json`

조직개편으로 자주 바뀌므로 반드시 별도 파일로 분리하고, 다른 데이터는 ID로만 참조한다.

```json
{
  "as_of": "2026-09-01",
  "source_url": "https://...",
  "companies": [
    {
      "id": "hanwha-aerospace",
      "name_ko": "한화에어로스페이스",
      "sector": "방산/항공우주",
      "divisions": [
        {
          "id": "hanwha-aerospace.defense",
          "name_ko": "지상방산부문",
          "summary": "장갑차·자주포 등 지상 무기체계 개발 및 양산",
          "key_products": ["K9 자주포", "레드백"],
          "counterparties": ["방위사업청", "국방과학연구소"],
          "status": "active"
        }
      ]
    }
  ]
}
```

- `status`: `active` | `reorganized` | `merged`. 폐지된 부문도 삭제하지 말고 상태만 변경 (과거 공고 참조 대응)
- `as_of`, `source_url` 필수. 답변 시 기준일자 노출에 사용

### 1.2 직군 마스터 — `/data/job_families.json`

부문과 무관한 **공통 설명**. 1회 작성 후 전 계열사에서 재사용한다.

```json
{
  "id": "finance",
  "name_ko": "재무",
  "roles": [
    {
      "id": "finance.treasury",
      "name_ko": "자금",
      "common": {
        "what_you_do": ["일별 자금수지 관리", "차입/상환 실행", "환헤지 포지션 관리"],
        "weekly_rhythm": "월초 자금계획 수립 → 일별 집행 → 월말 마감",
        "collaborates_with": ["회계", "IR", "사업부 기획"],
        "external_counterparties": ["시중은행", "증권사 IB", "신용평가사"],
        "required_skills": ["엑셀 재무모델링", "회계 기초"],
        "preferred_skills": ["CFA/CPA", "외국어(영어)", "ERP(SAP) 경험"],
        "related_majors": ["경영", "경제", "회계"],
        "career_path": {
          "y3": "단일 법인 자금집행 실무 전담",
          "y7": "그룹 단위 자금계획 수립 / 조달 구조 설계"
        },
        "not_a_fit_if": [
          "정해진 마감 사이클의 반복 업무에 스트레스를 크게 받는 경우",
          "숫자 검증보다 아이디어 발산 중심으로 일하고 싶은 경우"
        ],
        "scenario_seeds": [
          {
            "trigger": "월말 자금수지 마감일에 사업부 예상 매출채권 회수가 3일 지연 통보됨",
            "your_move": "단기 차입 한도 확인 → 지급 우선순위 재조정 → 재무팀장 보고",
            "skill_used": ["자금수지 관리", "은행 커뮤니케이션"]
          }
        ],
        "interview_themes": [
          "숫자 오류를 발견했을 때 어떻게 검증하고 보고했는가",
          "마감 압박 상황에서 우선순위를 조정한 경험"
        ]
      }
    }
  ]
}
```

`scenario_seeds`와 `interview_themes`는 §4.1과 §4.3의 생성 근거가 된다. 이 필드가 비어 있으면 해당 기능이 환각으로 채워지므로, 직무 데이터 작성 시 **필수**로 취급한다.

`not_a_fit_if` 필드를 반드시 채울 것. 지원자의 실질적 관심사는 "이 부서가 뭘 하나"가 아니라 "내가 여기 맞나"이며, 이 필드가 체감 만족도를 좌우한다.

### 1.3 교차 셀 — `/data/positions/{company}.{division}.{role}.json`

전 조합을 채우면 양이 폭발한다(계열사 10 × 부문 3 × 직무 8 ≈ 240셀). **차이점만(delta) 기술**한다.

```json
{
  "id": "hanwha-aerospace.defense.finance.treasury",
  "company_id": "hanwha-aerospace",
  "division_id": "hanwha-aerospace.defense",
  "role_id": "finance.treasury",
  "delta": {
    "what_differs": "정부 장기계약 특성상 원가검증 대응과 선수금 관리 비중이 일반 제조업 대비 높다",
    "division_specific_skills": ["방위사업 원가계산규칙 이해"],
    "day_in_life": "현직자 인터뷰 기반 서술 (3~5문장)",
    "typical_projects": ["대형 수출계약 대금 스케줄 설계"]
  },
  "hiring_history": [
    { "year": 2025, "half": 1, "posted": true }
  ],
  "content_owner": "인사팀 담당자명",
  "last_reviewed": "2026-08-15"
}
```

**작성 우선순위**: 최근 3년 내 `hiring_history.posted == true`인 셀부터. 나머지는 직군 공통 설명 + fallback 안내로 처리.

### 1.4 스키마 검증

`/schemas/*.json`에 JSON Schema를 두고, CI에서 `pytest tests/test_schema.py`로 전 데이터 파일을 검증한다. 필수 필드 누락 시 빌드 실패.

---

## 2. 디렉토리 구조

```
/
├── SPEC.md
├── data/
│   ├── organizations.json
│   ├── job_families.json
│   ├── issues/
│   └── positions/
├── schemas/
├── backend/
│   ├── main.py                 # FastAPI 엔트리
│   ├── retrieval/
│   │   ├── indexer.py          # 데이터 → 벡터 인덱스
│   │   └── retriever.py        # 하이브리드 검색 (BM25 + 임베딩)
│   ├── chat/
│   │   ├── prompts.py          # 시스템 프롬프트 (§5)
│   │   ├── guardrails.py       # 차단 필터 (§6)
│   │   └── router.py           # 의도 분류 → 핸들러 분기
│   ├── features/
│   │   ├── scenario.py         # 업무 시나리오 변환 (§4.1)
│   │   ├── gap_analysis.py     # 이력서 역량 갭 분석 (§4.2)
│   │   ├── interview.py        # 면접 예상 질문 (§4.3)
│   │   └── issues.py           # 직무 이슈 분석 (§4.4)
│   ├── profile/
│   │   ├── parser.py           # 이력서 → 경험 단위 구조화
│   │   ├── redact.py           # 민감정보 마스킹 (LLM 호출 전 필수)
│   │   └── skill_synonyms.json # 역량 동의어 사전
│   └── logging/
│       └── metrics.py          # fallback 로그 등 (§7)
├── frontend/
└── tests/
```

---

## 3. 검색 및 답변 생성

### 3.1 인덱싱

- 청크 단위: **필드 단위**로 쪼갠다 (문서 통째로 넣지 말 것). `what_you_do` 한 덩어리, `not_a_fit_if` 한 덩어리 식.
- 각 청크 메타데이터에 `company_id`, `division_id`, `role_id`, `axis`(`org` | `function` | `cross`), `last_reviewed` 부착.
- 채용공고 원문 PDF/HTML은 별도 컬렉션으로 인덱싱하고 `source_url`, `posted_date` 필수 부착.

### 3.2 질의 라우팅

지원자 진입 경로는 두 갈래이며, 한 축으로만 트리를 짜면 나머지 절반에 답하지 못한다.

| 질문 유형 | 예시 | 검색 필터 |
|---|---|---|
| 조직축 진입 | "태양광 쪽 일하고 싶어요" | `axis in (org, cross)`, division 우선 |
| 기능축 진입 | "재무 직무가 궁금해요" | `axis in (function, cross)`, role 우선 |
| 교차 질의 | "한화솔루션 큐셀 해외영업은?" | `axis = cross`, 정확 매칭 |
| 비교 질의 | "영업이랑 마케팅 차이는?" | 복수 role 병렬 검색 후 대조 생성 |

의도 분류는 LLM 1-shot 분류로 처리하되, 명확한 고유명사 매칭은 룰 기반 선처리로 비용을 절감한다.

### 3.3 답변 포맷

- 기본 3~5문장. 상세는 "더 보기" 확장.
- 답변 하단에 **출처 + 기준일자** 고정 노출.
- 답변 말미에 다음 질문 후보 2~3개 제시 (이탈률 방지).
- 교차 셀 데이터가 없으면: 직군 공통 설명 제공 → "해당 부문 특화 정보는 아직 준비 중" 명시 → fallback 로그 기록.

---

## 4. 핵심 기능 4종

이 네 기능이 "채용 홈페이지를 왜 두고 챗봇을 쓰는가"에 대한 답이다. 사용자는 특정 계열사·직무를 지정한 뒤 **버튼으로 네 기능에 진입**한다(§4.5).

```
[4.1 업무 시나리오]  →  [4.2 역량 갭 분석]  →  [4.3 면접 예상 질문]
 무슨 일을 하는가       나는 무엇이 부족한가     그래서 뭘 물어볼 것인가
                              ↑
                    [4.4 직무 이슈 분석]
                     지금 이 직무의 현안
```

4.1 → 4.2 → 4.3은 **직렬로 연결**된다. 앞 단계 출력을 뒷 단계 입력으로 넘겨야 한다. 각각을 독립 기능으로 만들면 답변이 서로 따로 놀아 체감 품질이 급락한다. 4.4는 독립 진입이 가능하되 4.3의 질문 생성에 재료로 투입된다.

세션 컨텍스트에 `active_company_id`, `active_position_id`, `profile_summary`를 유지할 것.

---

### 4.1 업무 시나리오 변환

**문제**: `what_you_do`의 "자금수지 관리"는 지원자에게 아무 정보도 주지 않는다. 추상적 R&R을 실제 업무 상황으로 변환해야 한다.

**입력**: `position_id` + `role.scenario_seeds` + `division.delta`

> **갱신 (`prompt_function.txt` 반영, 2026-09)**: 출력 포맷을 표 2개로 변경했다. `scenario_seeds` 기반 그라운딩은 폐기했다 — `data/job_families.json`이 존재하지 않아 모든 직무가 항상 fallback되는 문제가 있었다. 대신 회사명+직무명만으로 `generateGroundedText`(§0 참고, 실제로는 검색 없이 모델의 일반 지식을 쓰는 `generateText` 래퍼)를 호출해 통상적인 JD 패턴을 설명하게 한다. 실시간 검색이 아니므로 `sources`는 항상 빈 배열이고, 응답 텍스트가 비었을 때만(fallback) "해당 직무의 상세 사례는 준비 중입니다"로 처리한다.

**출력 포맷** (고정)

```
### 업무 Flow
| 단계 | 내용 |
|---|---|
| ... | ... |

### 하루 일과 업무 시나리오
| 시간대 | 업무 내용 |
|---|---|
| ... | ... |
```

**구현 규칙**
- 검색 그라운딩이 없으므로 특정 회사의 내부 정보를 아는 것처럼 단정하지 말고, 확실하지 않은 내용은 통상적인 직무 패턴 수준의 일반론으로 서술하도록 시스템 프롬프트로 지시한다. 모델 응답이 비어 있을 때만 "해당 직무는 상세 사례 준비 중"으로 fallback.
- 난이도 2단계 제공: `기본`(신입 1년차가 맡는 일) / `심화`(3년차 이후).
- `어려운 점`을 반드시 포함한다. 미화된 서술만 있으면 입사 후 기대 불일치로 이어지고, 이는 조기 퇴사 원인이 된다.
- 시나리오는 **관찰용**이지 시험이 아니다. 지원자에게 답을 요구하고 채점하는 형태로 만들지 말 것(§6).

---

### 4.2 이력서 기반 역량 갭 분석

**입력**: 지원자가 붙여넣은 자기소개서 텍스트(필수) + 포트폴리오 텍스트(선택) + 대상 `position_id`

> **갱신 (`prompt_function.txt` 반영)**: 4문항 간이 프로필 대체 입력은 제거하고 자기소개서(필수)/포트폴리오(선택) 텍스트 입력으로 변경했다. `required_skills`/`preferred_skills` 매칭 게이팅은 제거하고, LLM이 직무명 기준 JD 요구사항을 일반 지식으로 판단하도록 했다(자유 대화 답변과 동일한 방식).

**처리 순서**
1. 지원자가 제공한 경험을 상황/역할/문제/행동/사용한 기술 또는 지식/결과/성과/배운 점으로 구조화한다. 제공되지 않은 경험·성과는 만들어내지 않는다.
2. 각 경험이 해당 직무의 어떤 역량을 증명하는지 분석한다 (직무 연관성 / 활용 가능한 역량 / 추천 활용도: 높음·중간·낮음 / 자기소개서 활용 포인트 / 면접 활용 포인트).
3. 강점 / 부족한 역량 / 보유하나 표현되지 않은 역량으로 분류한다.

**출력 포맷** (고정)

```
[경험 분석] (경험명)   상황/역할/문제/행동/기술/결과/성과/배운 점
[역량 매핑] (경험명)   직무 연관성/활용 가능한 역량/추천 활용도/자소서 포인트/면접 포인트
[강점]                 근거 경험 + 연관 요구사항 + 자소서 표현 제안
[부족한 역량]
[보유하나 표현되지 않은 역량]
```

각 항목은 500자 이내로 작성한다.

**구현 규칙 (중요)**
- 점수·백분율·합격 가능성·순위 출력 **전면 금지**. "적합도 78%" 같은 표현은 법적 리스크이자 신뢰 붕괴 요인이다.
- 강점 판정에는 반드시 **경험 근거**를 함께 제시한다. 근거 없는 칭찬은 기능을 무의미하게 만든다.
- `부족한 역량` 항목도 지적으로 끝내지 않는다.
- 자기소개서/포트폴리오 텍스트는 **서버 영속 저장 금지**. 세션 메모리에만 두고 종료 시 폐기. §6 참조.
- 학교명·생년·성별·사진·주소는 파싱 단계에서 **즉시 마스킹**하고 LLM에 전달하지 않는다. 프롬프트에 들어가면 편향 판단이 발생할 수 있다.
- 자기소개서가 없으면 `needs_profile` 상태로 응답하고 프론트에서 입력 단계를 먼저 띄운다.

---

### 4.3 면접 예상 질문 생성

**입력**: 대상 직무 정보 + 세션에 저장된 자기소개서(있으면)

> **갱신 (`prompt_function.txt` 반영)**: 4개 유형·비율 구성 대신 "JD 기반 5개 + 개인 역량 기반 5개"의 2분류·표 형식으로 변경했다. 개인 역량 기반 질문은 자기소개서가 있을 때만 생성한다. 4.4(이슈 분석)와의 연동은 제거했다.

**출력 포맷** (고정, 표 2개)

```
### JD 기반 예상 질문
| 질문 | 질문 의도 / JD 근거 |
|---|---|

### 개인 역량 기반 예상 질문 (자기소개서 제출 시)
| 질문 | 연결되는 경험/근거 |
|---|---|
```

**구현 규칙**
- **모범 답안 전문을 작성해주지 않는다.** 구조와 방향까지만. 4.2의 대필 금지 원칙과 동일한 이유다.
- "실제 한화 면접 기출"이라고 표기하지 않는다. 모든 질문 블록은 `예상 질문`으로 라벨링한다. 검증되지 않은 기출 주장은 허위 정보다.
- 자기소개서가 없어도 JD 기반 5개는 정상 생성한다(차단 금지). 이 경우 "자기소개서를 제출하시면 개인 역량 기반 질문도 함께 생성됩니다" 안내를 붙인다.
- 지원자가 제공하지 않은 경험은 만들어내지 않는다.

---

### 4.4 직무 이슈 분석

**목적**: 해당 계열사·직무가 **지금** 직면한 산업/규제/시장 현안과, 그것이 실무에 미치는 영향을 설명한다. 지원자가 면접에서 "요즘 업계 이슈를 어떻게 보느냐"에 답할 재료가 된다.

**입력**: `company.sector`, `division.key_products`, `role` + 인덱싱된 뉴스·공시·업계 리포트

> **갱신 (`prompt_function.txt` 반영, 2026-09)**: `/data/issues/{company_id}.json` 정적 파일 기반 구조는 폐기했다 — 실제로 해당 파일이 하나도 없어 모든 회사/직무 조합이 항상 fallback되는 문제가 있었다. 대신 회사명+직무명을 `generateText`(Gemini, 실시간 검색 없음)에 넘겨 "회사 이슈"/"직무 이슈" JSON을 받는다. 시스템 프롬프트로 특정 기사 제목·보도일자·URL을 지어내지 말고, 사전학습된 일반적인 산업 동향 수준으로만 답하도록 지시한다(`backend/features/issues.js`의 `SYSTEM`). 출력은 **회사 이슈**(`company_issues`) / **직무 이슈**(`job_issues`)로 구분해서 표시하며, `sources`는 항상 빈 배열이다. 모델이 스스로 `insufficient: true`로 답하거나 두 목록이 모두 비면 안내 문구 `"현재 '{company} / {job}'와 직접적으로 관련된 신뢰할 수 있는 최신 자료가 충분하지 않습니다."`를 고정 출력한다.

**출력 포맷** (고정)

```
[회사 이슈] 또는 [직무 이슈]
[이슈]        한 문장 요약
[배경]        왜 이런 상황이 되었는가 (2~3문장)
[실무 영향]   이 직무의 업무가 구체적으로 어떻게 바뀌는가
[면접 관점]   면접에서 물어볼 만한 각도 + 흔한 오답
```

**구현 규칙 (이 기능이 환각 위험이 가장 높다)**
- **실시간 검색이 없으므로 특정 기사·보도일자·URL을 요구하거나 표시하지 않는다.** 대신 이 내용이 "일반적인 산업 지식"이지 실시간 뉴스가 아니라는 안내 문구를 답변 최상단에 고정 노출한다(`issues.js`의 `[안내]` 블록).
- 모델이 확신하지 못하면 `insufficient: true`로 답하도록 시스템 프롬프트로 지시하고, 그 경우 "관련 자료 준비 중" 문구로 fallback한다. 다만 이는 모델 자체의 준수에 의존하며 코드 레벨의 하드 검증은 아니다.
- **미공개 정보·내부 전략 추측 금지.** 공시·보도된 사실과 그에 대한 일반적 해석까지만. "한화가 OO를 인수할 것으로 보인다" 같은 서술은 차단(§6). 시스템 프롬프트(`backend/features/issues.js`의 `SYSTEM`)에 명시돼 있으나, 이 역시 모델 자체의 준수에 의존하므로 코드 레벨의 하드 차단은 아니다.
- 실적·주가 전망, 투자 판단으로 읽힐 서술 금지. 이 챗봇은 채용 안내용이지 리서치가 아니다.

**알려진 한계**: `/data/issues/{company_id}.json` 기반 정적 데이터 모델(사업부/역할 매핑, `sensitivity` 플래그, `reviewed_by` 인사팀 검수)은 폐기했다. 검색 그라운딩 없는 일반 지식 기반 응답으로 전환하면서 **구조조정·소송·사고·규제 제재 같은 민감 이슈를 인사팀이 사전 검수하는 절차가 코드에 더 이상 없다** — 모델이 학습 데이터에 있던 그런 이슈를 언급하면 그대로 노출된다. 또한 실시간 정보 없이 답하므로 사실관계가 실제보다 오래됐거나 부정확할 수 있다. 민감 이슈 노출을 다시 통제하려면 §6 가드레일에 민감 키워드 기반 사후 필터를 추가하거나, 정확도가 중요해지면 Gemini `googleSearch` 툴로 실제 검색 그라운딩을 켜는 것을 고려한다.

---

### 4.5 버튼 기반 진입 UI

사용자가 **계열사 + 직무를 특정한 시점**에 네 기능 버튼을 노출한다. 자유 입력만 두면 지원자는 무엇을 물어야 할지 모른다.

**트리거 조건**
- `active_company_id`와 `active_position_id`(또는 `role_id`)가 모두 확정됨
- 하나라도 미확정이면 버튼 대신 선택 질문을 먼저 띄운다

**버튼 구성** (순서 고정)

| 순서 | 라벨 | 핸들러 | 사전조건 |
|---|---|---|---|
| 1 | 직무 설명 | §4.1 | 없음 |
| 2 | 나의 역량 진단하기 | §4.2 | 이력서 또는 간이 프로필 |
| 3 | 직무 이슈 분석 | §4.4 | 해당 이슈 데이터 존재 |
| 4 | 면접 예상 질문 | §4.3 | 없음 (2·3 선행 시 품질 향상) |

**동작 규칙**
- 버튼은 답변 하단에 상시 유지한다. 한 번 쓰고 사라지면 재진입 경로가 없어진다.
- 이미 조회한 버튼은 완료 표시하되 **비활성화하지 않는다**.
- 2번 클릭 시 이력서/프로필이 없으면 먼저 입력 단계를 띄운다.
- 3번은 이슈 데이터가 없으면 버튼을 노출하되 클릭 시 fallback 안내 + `issue_missing` 로그.
- 4번 클릭 시 2·3 미실행이면 "먼저 진단하면 더 정확해집니다" 안내를 붙이되 **차단하지는 않는다**.
- 직무를 바꾸면 세션 컨텍스트를 초기화하고 버튼을 새로 그린다. 이전 직무의 갭 분석을 새 직무에 재사용하지 말 것.

---

## 5. 시스템 프롬프트 요건

`backend/chat/prompts.py`에 다음을 반드시 포함한다.

- 인덱싱된 컨텍스트에 없는 내용은 추측하지 않고 "확인되지 않았다"고 답한다.
- 초봉, 성과급, 합격 커트라인, 학벌 선호 여부는 답하지 않고 공식 채용 페이지로 안내한다.
- 자기소개서는 **방향성 코칭까지만**. 문항에 대한 완성 문장 대필은 거부한다. (전형 공정성 이슈)
- 지원자를 평가하거나 당락을 시사하는 표현을 쓰지 않는다.
- 조직 정보는 항상 `as_of` 기준일자를 함께 밝히고, 조직개편 가능성을 부기한다.

---

## 6. 가드레일 (`guardrails.py`)

**입력 차단**
- 특정 개인의 합격 여부 문의
- 성별·연령·출신지·학교 서열 관련 판단 요구
- 타 지원자 정보 요구

**출력 차단 (정규식 + LLM 판정 2단)**
- `합격 가능성 \d+%`, `무조건`, `보장` 등 확정적 표현
- 특정 학교/전공 우대 단정
- 급여 수치 (공식 공고 인용 제외)

- 검증되지 않은 "실제 기출 문항" 주장 (§4.3)
- 이력서 기반 적합도 점수·순위·확률 (§4.2)
- 미공개 경영 정보 추측, 인수·구조조정 전망, 실적·주가 예측 (§4.4)
- 출처 없는 업계 이슈 서술 (§4.4)

**이력서 처리 (`profile/redact.py`)**

수집 최소화가 아니라 **수집 회피**가 원칙이다. 갭 분석에 필요한 것은 경험과 역량뿐이며, 나머지는 편향의 원인일 뿐이다.

| 항목 | 처리 |
|---|---|
| 학교명, 학과 | 학과만 계열로 일반화, 학교명 삭제 |
| 생년월일, 나이 | 삭제 |
| 성별, 사진 | 삭제 (이미지 업로드 자체를 미지원) |
| 주소, 연락처 | 삭제 |
| 경력 기간 | 연 단위로만 유지 |

- 마스킹은 **LLM 호출 전** 단계에서 수행한다. 프롬프트에 들어간 뒤의 필터링은 무의미하다.
- 원문 이력서는 세션 메모리에만 보관, 서버 영속 저장 및 로그 기록 금지.
- 업로드 화면에 보관 정책을 명시하고, 즉시 삭제 버튼을 제공한다.

**개인정보**
- 진단 응답은 세션 종료 시 폐기. 서버 영속 저장 금지.
- 대화 로그는 익명 세션 ID로만 저장. 이름·연락처 입력 시 마스킹 후 저장.
- 4.2/4.3 사용 로그는 **집계 수치만** 남긴다 (호출 횟수, 직무 분포). 입력 내용은 남기지 않는다.

---

## 7. 로깅 및 지표 (`metrics.py`)

| 지표 | 정의 | 용도 |
|---|---|---|
| `fallback_rate` | 교차 셀 미보유로 공통 설명만 나간 비율 | **콘텐츠 보강 1순위 소스** |
| `session_turns` | 세션당 질문 수 | 대화 설계 품질 |
| `unanswered_queries` | 라우팅 실패 질의 원문 | 신규 필드 발굴 |
| `position_detail_view` | 직무 상세 확장 클릭률 | 콘텐츠 유효성 |
| `scenario_seed_missing` | 시나리오 시드 부재로 fallback한 직무 | §4.1 콘텐츠 갭 |
| `feature_funnel` | 시나리오 → 갭분석 → 면접질문 단계별 이탈 | 3종 연결 동선 검증 |
| `apply_click` | 공고 링크 이탈 | 최종 전환 |

`fallback_rate`와 `unanswered_queries`는 **1단계부터** 수집한다. 초기 콘텐츠 갭을 메우는 유일한 근거다.

---

## 8. 구현 단계

**Phase 1 — 데이터 골격**
- [ ] `schemas/` JSON Schema 작성
- [ ] `organizations.json` 초안 (계열사 5개, 채용 실적 상위)
- [ ] `job_families.json` 초안 (직군 8개 × 세부직무 2~3개)
- [ ] 스키마 검증 테스트 통과

**Phase 2 — 검색 파이프라인**
- [ ] 필드 단위 청킹 + 메타데이터 부착 인덱서
- [ ] 하이브리드 리트리버 (BM25 + 임베딩)
- [ ] 축(axis)별 필터링 검증 테스트

**Phase 3 — 대화 계층**
- [ ] 의도 분류 라우터
- [ ] 시스템 프롬프트 + 가드레일
- [ ] 출처/기준일자 자동 부착

**Phase 4 — 핵심 기능 4종**
- [ ] `redact.py` 마스킹 + 단위 테스트 (**다른 무엇보다 먼저**)
- [ ] `scenario.py` — 시드 없을 때 fallback 동작 검증
- [ ] `parser.py` + 동의어 사전 초안
- [ ] `gap_analysis.py` — 점수 출력 차단 테스트 포함
- [ ] `issues.py` — 출처 부재 시 생성 거부, `expires_at` 필터링 검증
- [ ] `interview.py` — 갭 분석 결과 연동 및 단독 호출 양쪽 검증
- [ ] 세션 컨텍스트(`active_company_id`, `active_position_id`, `profile_summary`) 유지 로직

**Phase 5 — 프론트엔드**
- [ ] 추천 질문 버튼 진입 (빈 입력창 금지)
- [ ] **§4.5 4버튼 메뉴 컴포넌트** — 상시 유지, 완료 표시, 직무 변경 시 초기화
- [ ] 답변 + 출처 + 다음 질문 후보 UI
- [ ] 이력서 입력 UI + 보관 정책 고지 + 즉시 삭제 버튼
- [ ] 시나리오 → 갭 분석 → 면접 질문 연결 동선

**Phase 6 — 운영**
- [ ] 지표 대시보드
- [ ] 콘텐츠 오너별 `last_reviewed` 만료 알림 (180일)
- [ ] 채용 시즌 전 갱신 체크리스트

---

## 9. 유지보수 규칙

- 부문 명칭을 코드나 프롬프트에 **하드코딩 금지**. 반드시 `organizations.json` 참조.
- 계열사별 실제 직무명도 **하드코딩 금지**. `data/company_jobs.json`(출처: `Job_name.txt`)에서 조회하며, 이 파일에 없는 계열사는 `organizations.json`에서도 제외한다. 직무명을 갱신할 때는 `Job_name.txt` → `company_jobs.json` 순으로 반영한다.
- `last_reviewed`가 180일 초과한 `position` 파일은 답변 시 "정보 갱신 필요" 내부 플래그 부착.
- 채용 공고는 매 시즌 변경된다. 갱신 주기와 콘텐츠 오너를 사전에 확정하지 않으면 2년 전 정보가 남아 오히려 신뢰를 깎는다.

---

## 10. Claude Code 작업 지시 예시

```
SPEC.md의 §1을 읽고 schemas/ 아래에 organizations, job_families,
position 3개의 JSON Schema를 작성해줘. 그다음 tests/test_schema.py에
data/ 아래 모든 파일을 검증하는 테스트를 추가해줘.
```

```
SPEC.md §3.1에 따라 backend/retrieval/indexer.py를 구현해줘.
청킹은 문서 단위가 아니라 필드 단위여야 하고, 메타데이터에
axis 필드가 반드시 들어가야 해.
```

```
SPEC.md §6의 이력서 처리 표에 따라 profile/redact.py를 먼저 구현하고,
마스킹 누락을 잡는 테스트를 작성해줘. 학교명/생년/성별/주소가
LLM 프롬프트에 도달하면 테스트가 실패해야 해.
```

```
SPEC.md §4.2와 §4.3을 읽고 gap_analysis.py, interview.py를 구현해줘.
갭 분석 출력에 점수나 백분율이 포함되면 안 되고,
면접 질문은 반드시 '예상 질문'으로 라벨링되어야 해.
```
